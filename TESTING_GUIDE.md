# 🧪 Руководство по тестированию улучшений

## Шаг 1: Запуск Backend

Откройте **первый терминал**:

```bash
cd /Users/a1111/usm-service

# Вариант A: Использовать скрипт (рекомендуется)
./start-backend.sh

# Вариант B: Вручную
cd backend
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
export OPENAI_API_KEY=sk-your-key-here  # ⚠️ ВАЖНО!
python main.py
```

**Что проверить:**
- ✅ Сервер запустился на http://127.0.0.1:8000
- ✅ Откройте http://127.0.0.1:8000/docs - должна открыться Swagger документация
- ✅ В логах видно структурированные сообщения (если LOG_LEVEL=INFO)

## Шаг 2: Запуск Frontend

Откройте **второй терминал** (новый):

```bash
cd /Users/a1111/usm-service

# Вариант A: Использовать скрипт
./start-frontend.sh

# Вариант B: Вручную
cd frontend
npm install
npm run dev
```

**Что проверить:**
- ✅ Frontend запустился на http://localhost:5173
- ✅ Откройте в браузере - должна открыться форма ввода

## Шаг 3: Тестирование основных функций

### 3.1. Тест валидации (новое!)

1. **Слишком короткий текст:**
   - Введите меньше 10 символов (например: "test")
   - Нажмите "Сгенерировать карту"
   - ✅ Должна появиться ошибка: "Текст слишком короткий. Минимум 10 символов"

2. **Слишком длинный текст:**
   - Введите больше 10000 символов
   - ✅ Должен появиться счетчик с предупреждением
   - ✅ Кнопка должна быть неактивна

3. **Правильный размер:**
   - Введите текст от 10 до 10000 символов
   - ✅ Счетчик показывает "X / 10000 символов"
   - ✅ Кнопка активна

### 3.2. Тест автосохранения (новое!)

1. Введите текст в поле
2. Подождите 1 секунду
3. Обновите страницу (F5)
4. ✅ Текст должен сохраниться (восстановится из localStorage)

### 3.3. Тест прогресс-бара (новое!)

1. Введите валидный текст (например, из примера ниже)
2. Нажмите "Сгенерировать карту"
3. ✅ Должен появиться прогресс-бар
4. ✅ Процент должен увеличиваться (10% → 70% → 100%)
5. ✅ Под прогресс-баром текст: "AI анализирует требования... X%"

### 3.4. Тест генерации карты

**Пример текста для тестирования:**
```
Приложение для библиотеки. Есть роли: Читатель и Библиотекарь. 
Читатель может искать книги по автору и названию, бронировать книги, 
брать их на дом и продлевать срок. Библиотекарь выдает книги читателям, 
принимает возвраты, начисляет штрафы за просрочку и управляет каталогом.
```

1. Вставьте текст выше
2. Нажмите "Сгенерировать карту"
3. Подождите 20-40 секунд
4. ✅ Должна появиться карта с:
   - Activities (синие заголовки сверху)
   - User Tasks (под Activities)
   - Releases (MVP, Release 1, Later - слева)
   - User Stories (желтые карточки)

### 3.5. Тест обработки ошибок (новое!)

**Тест без API ключа:**
1. Остановите backend (Ctrl+C)
2. Запустите без `OPENAI_API_KEY`:
   ```bash
   cd backend
   source venv/bin/activate
   unset OPENAI_API_KEY
   python main.py
   ```
3. Попробуйте сгенерировать карту
4. ✅ Должна появиться понятная ошибка: "OpenAI API key not configured"

**Тест с неправильным API ключом:**
1. Установите неправильный ключ: `export OPENAI_API_KEY=sk-wrong`
2. Попробуйте сгенерировать карту
3. ✅ Должна появиться детальная ошибка от OpenAI API

## Шаг 4: Тестирование через API (Swagger)

1. Откройте http://127.0.0.1:8000/docs
2. Найдите эндпоинт `POST /generate-map`
3. Нажмите "Try it out"
4. Введите JSON:
   ```json
   {
     "text": "Приложение для доставки еды. Клиент заказывает, курьер доставляет."
   }
   ```
5. Нажмите "Execute"
6. ✅ Должен вернуться `project_id`
7. Используйте `GET /project/{project_id}` для получения карты

## Шаг 5: Запуск тестов

В **третьем терминале**:

```bash
cd /Users/a1111/usm-service/backend
source venv/bin/activate
pytest test_main.py -v
```

**Что должно быть:**
- ✅ Все тесты проходят (8 тестов)
- ✅ Видны результаты каждого теста
- ✅ Нет ошибок

**Примерные тесты:**
- `test_generate_map_no_api_key` - проверка без API ключа
- `test_generate_map_empty_text` - проверка пустого текста
- `test_generate_map_short_text` - проверка короткого текста
- `test_generate_map_long_text` - проверка длинного текста
- `test_get_project_not_found` - проверка несуществующего проекта
- `test_list_projects_empty` - проверка пустого списка
- `test_generate_map_rate_limit` - проверка обработки rate limit
- `test_generate_map_timeout` - проверка обработки timeout

## Шаг 6: Проверка улучшений безопасности

### CORS настройки

1. Откройте консоль браузера (F12)
2. Попробуйте сделать запрос с другого домена (если есть)
3. ✅ Должна быть ошибка CORS (если домен не в ALLOWED_ORIGINS)

### Логирование

1. Посмотрите логи backend в терминале
2. ✅ Должны быть структурированные логи:
   - `INFO - Generating map for requirements (length: X chars)`
   - `INFO - Successfully received AI response`
   - Или ошибки с деталями

## Шаг 7: Тестирование Docker (опционально)

```bash
cd /Users/a1111/usm-service

# Установите API ключ
export OPENAI_API_KEY=sk-your-key-here

# Запустите через Docker
docker-compose up
```

**Что проверить:**
- ✅ Backend доступен на http://localhost:8000
- ✅ Frontend доступен на http://localhost:5173
- ✅ Всё работает как обычно

## Чек-лист тестирования

### Критические функции
- [ ] Валидация короткого текста (< 10 символов)
- [ ] Валидация длинного текста (> 10000 символов)
- [ ] Счетчик символов работает
- [ ] Прогресс-бар появляется и обновляется
- [ ] Автосохранение работает (обновить страницу)
- [ ] Генерация карты работает с валидным текстом
- [ ] Карта отображается корректно
- [ ] Ошибки отображаются понятно

### Безопасность
- [ ] CORS работает (проверка в консоли браузера)
- [ ] Ошибки OpenAI обрабатываются корректно
- [ ] Логирование работает

### Тесты
- [ ] Все тесты проходят (`pytest test_main.py -v`)
- [ ] Нет ошибок в тестах

### UX
- [ ] Прогресс-бар виден
- [ ] Валидация работает
- [ ] Сообщения об ошибках понятны
- [ ] Интерфейс отзывчивый

## Что делать, если что-то не работает

1. **Backend не запускается:**
   - Проверьте Python версию: `python3 --version` (нужна 3.8+)
   - Проверьте зависимости: `pip install -r requirements.txt`
   - Проверьте API ключ: `echo $OPENAI_API_KEY`

2. **Frontend не запускается:**
   - Проверьте Node.js: `node --version` (нужна 18+)
   - Установите зависимости: `npm install`
   - Проверьте порт 5173: не занят ли он

3. **Ошибки при генерации:**
   - Проверьте API ключ OpenAI
   - Проверьте баланс OpenAI аккаунта
   - Посмотрите логи backend

4. **Тесты не проходят:**
   - Убедитесь, что установлен pytest: `pip install pytest`
   - Проверьте, что все зависимости установлены

## Полезные команды

```bash
# Проверить статус git
git status

# Посмотреть логи backend (если запущен)
# Логи видны прямо в терминале

# Проверить переменные окружения
env | grep OPENAI

# Проверить порты
lsof -i :8000  # Backend
lsof -i :5173  # Frontend
```

Удачи в тестировании! 🚀

