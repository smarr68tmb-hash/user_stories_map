# 🚀 Быстрый старт

## Шаг 1: Настройка Backend

```bash
cd backend

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Установите API ключи AI (по умолчанию приоритет: gemini → groq → perplexity → openai)
export GEMINI_API_KEY=your-gemini-key-here   # приоритетный
export GROQ_API_KEY=gsk-your-api-key-here     # fallback 1
export PERPLEXITY_API_KEY=pplx-your-api-key-here # fallback 2
export OPENAI_API_KEY=sk-your-api-key-here    # fallback 3

# Настройка приоритета (опционально, если нужен иной порядок)
# export AI_PROVIDER_PRIORITY="gemini,groq,perplexity,openai"

# Запустите сервер
python main.py
```

Backend будет доступен на **http://127.0.0.1:8000**

## Шаг 2: Настройка Frontend

Откройте **новый терминал**:

```bash
cd frontend

# Установите зависимости (требуется Node.js и npm)
npm install

# Запустите dev сервер
npm run dev
```

Frontend будет доступен на **http://localhost:5173**

## Шаг 3: Использование

1. Откройте браузер и перейдите на http://localhost:5173
2. Введите описание вашего продукта в текстовое поле
3. Нажмите "Сгенерировать карту"
4. Подождите 20-40 секунд
5. Изучите сгенерированную карту пользовательских историй!

## Пример текста для тестирования:

```
Приложение для доставки еды. Есть роли: Клиент и Курьер. 
Клиент может просматривать меню ресторанов, добавлять блюда в корзину, 
оформлять заказ с указанием адреса доставки и отслеживать статус заказа 
в реальном времени. Курьер получает уведомления о новых заказах, видит 
карту с маршрутом доставки, может принять или отклонить заказ, и отмечает 
доставку как выполненную с фото подтверждением.
```

## Альтернативный запуск (скрипты)

Или используйте готовые скрипты:

```bash
# Backend
./start-backend.sh

# Frontend (в другом терминале)
./start-frontend.sh
```

## 🔧 Устранение проблем

### Backend не запускается
- Убедитесь, что Python 3.8+ установлен
- Проверьте, что виртуальное окружение активировано
- Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`

### Frontend не запускается
- Убедитесь, что Node.js и npm установлены
- Проверьте, что зависимости установлены: `npm install`

### Ошибка "AI API key not configured"
- Установите хотя бы один API ключ (приоритет по умолчанию: gemini → groq → perplexity → openai):
  - `export GEMINI_API_KEY=your-gemini-key` (приоритетный)
  - `export GROQ_API_KEY=gsk-your-key` (fallback 1)
  - `export PERPLEXITY_API_KEY=pplx-your-key` (fallback 2)
  - `export OPENAI_API_KEY=sk-your-key` (fallback 3)
- Или создайте файл `.env` в папке backend, задав нужные ключи
- Система автоматически переключается между провайдерами при ошибках/лимитах

### Ошибка подключения к backend
- Убедитесь, что backend запущен на порту 8000
- Проверьте URL в `frontend/src/App.jsx` (должен быть `http://127.0.0.1:8000`)

## 📚 Дополнительная информация

- API документация: http://127.0.0.1:8000/docs
- База данных создается автоматически в `backend/usm.db`

