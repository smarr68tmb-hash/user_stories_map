# AI User Story Mapper

Сервис для автоматической генерации карты пользовательских историй (User Story Map) на основе текстовых требований с использованием AI.

## 🚀 Быстрый старт

### Backend

1. Перейдите в папку backend:
```bash
cd backend
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Установите OpenAI API ключ:
```bash
export OPENAI_API_KEY=sk-your-api-key-here
```

5. Запустите сервер:
```bash
python main.py
```

Backend будет доступен на http://127.0.0.1:8000

### Frontend

1. Перейдите в папку frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите dev сервер:
```bash
npm run dev
```

Frontend будет доступен на http://localhost:5173

## 📋 Функциональность

- ✅ Генерация User Story Map из текстовых требований
- ✅ Автоматическое выделение ролей (Personas)
- ✅ Создание структуры: Activities → User Tasks → User Stories
- ✅ Приоритизация историй (MVP, Release 1, Later)
- ✅ Генерация Acceptance Criteria
- ✅ Визуализация карты в виде интерактивной таблицы
- ✅ Сохранение проектов в базе данных
- ✅ Прогресс-бар и валидация входных данных
- ✅ Автосохранение черновиков
- ✅ Улучшенная обработка ошибок

## 🏗️ Архитектура

### Backend
- **FastAPI** - веб-фреймворк
- **SQLAlchemy** - ORM для работы с БД
- **SQLite** - база данных (для MVP)
- **OpenAI API** - генерация карты через GPT-4o

### Frontend
- **React** - UI библиотека
- **Vite** - сборщик
- **Tailwind CSS** - стилизация
- **Axios** - HTTP клиент

## 📁 Структура проекта

```
usm-service/
├── backend/
│   ├── main.py              # Основной файл FastAPI
│   ├── test_main.py         # Тесты
│   ├── requirements.txt     # Python зависимости
│   ├── .env.example         # Пример конфигурации
│   ├── Dockerfile           # Docker образ для backend
│   └── usm.db              # SQLite база данных (создается автоматически)
├── frontend/
│   ├── src/
│   │   ├── App.jsx         # Главный компонент
│   │   ├── main.jsx        # Точка входа
│   │   └── index.css       # Стили
│   ├── package.json        # Node зависимости
│   ├── vite.config.js      # Конфигурация Vite
│   └── Dockerfile          # Docker образ для frontend
├── docker-compose.yml       # Docker Compose конфигурация
├── start-backend.sh         # Скрипт запуска backend
├── start-frontend.sh        # Скрипт запуска frontend
├── push-to-github.sh        # Скрипт загрузки в GitHub
├── CHANGELOG.md            # История изменений
└── README.md
```

## 🔧 API Endpoints

- `POST /generate-map` - Генерация карты из текста
- `GET /project/{project_id}` - Получение проекта
- `GET /projects` - Список всех проектов
- `GET /docs` - Swagger документация

## 🎯 Пример использования

1. Откройте frontend в браузере
2. Введите описание продукта, например:
   ```
   Приложение для доставки еды. Есть роли: Клиент и Курьер. 
   Клиент может просматривать меню, добавлять блюда в корзину, 
   оформлять заказ и отслеживать доставку. Курьер получает 
   уведомления о новых заказах, видит маршрут и отмечает 
   доставку как выполненную.
   ```
3. Нажмите "Сгенерировать карту"
4. Дождитесь результата (20-40 секунд)
5. Изучите сгенерированную карту пользовательских историй

## 🐳 Docker

Запуск через Docker Compose:

```bash
# Создайте .env файл в корне проекта
export OPENAI_API_KEY=sk-your-key-here

# Запустите все сервисы
docker-compose up
```

Backend: http://localhost:8000  
Frontend: http://localhost:5173

## 🧪 Тестирование

Запуск тестов backend:
```bash
cd backend
pytest test_main.py -v
```

## 🔒 Безопасность

⚠️ **Важно для production:**
- Настройте `ALLOWED_ORIGINS` в `.env` на конкретные домены
- Не храните `.env` файлы в репозитории
- Используйте PostgreSQL вместо SQLite для production
- Настройте HTTPS

## 🔮 Планы развития

- [ ] Drag-and-drop для перемещения историй между релизами
- [ ] Редактирование историй прямо на карте
- [ ] Экспорт в Jira, Trello, CSV
- [ ] Загрузка файлов (PDF, DOCX)
- [ ] Генерация из скриншотов интерфейса
- [ ] Gap Analysis (поиск пропущенных функций)
- [ ] Совместное редактирование (real-time)
- [ ] Миграция на PostgreSQL
- [ ] Кеширование OpenAI запросов

## 📝 Лицензия

MIT

