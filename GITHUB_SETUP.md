# 📤 Загрузка проекта в GitHub

Проект готов к загрузке! Все файлы закоммичены.

## Вариант 1: Использование скрипта (рекомендуется)

```bash
./push-to-github.sh
```

Скрипт попросит ввести URL вашего репозитория и автоматически загрузит код.

## Вариант 2: Ручная настройка

### Шаг 1: Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Нажмите "New repository" (или перейдите по прямой ссылке: https://github.com/new)
3. Введите название репозитория (например: `usm-service` или `ai-user-story-mapper`)
4. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть в проекте)
5. Нажмите "Create repository"

### Шаг 2: Добавьте remote и загрузите код

GitHub покажет вам команды. Выполните их:

```bash
# Если репозиторий создан через HTTPS:
git remote add origin https://github.com/ВАШ_USERNAME/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git
git branch -M main
git push -u origin main

# Или если используете SSH:
git remote add origin git@github.com:ВАШ_USERNAME/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git
git branch -M main
git push -u origin main
```

### Шаг 3: Проверьте результат

Откройте ваш репозиторий на GitHub - все файлы должны быть там!

## 🔐 Аутентификация

Если при `git push` запрашивается пароль:

### Вариант A: GitHub Personal Access Token (HTTPS)
1. Создайте токен: https://github.com/settings/tokens
2. Используйте токен вместо пароля при push

### Вариант B: SSH ключи (рекомендуется)
1. Настройте SSH ключи: https://docs.github.com/en/authentication/connecting-to-github-with-ssh
2. Используйте SSH URL для remote

### Вариант C: GitHub CLI
```bash
# Установите GitHub CLI
brew install gh  # macOS
# или скачайте с https://cli.github.com

# Авторизуйтесь
gh auth login

# Создайте репозиторий и загрузите код
gh repo create usm-service --public --source=. --remote=origin --push
```

## 📝 Что уже в репозитории

✅ Весь исходный код (backend + frontend)  
✅ Конфигурационные файлы  
✅ README и документация  
✅ .gitignore (исключает venv, node_modules, .env, *.db)

❌ НЕ включено (и правильно):
- `venv/` - виртуальное окружение Python
- `node_modules/` - зависимости Node.js
- `.env` - секретные ключи
- `*.db` - база данных SQLite

## 🔄 Обновление кода в будущем

После изменений:

```bash
git add .
git commit -m "Описание изменений"
git push
```

## 💡 Полезные команды

```bash
# Проверить статус
git status

# Посмотреть историю коммитов
git log --oneline

# Посмотреть remote репозитории
git remote -v

# Изменить URL remote
git remote set-url origin НОВЫЙ_URL
```

