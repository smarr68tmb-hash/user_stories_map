# ✅ Чеклист запуска AI Agent MVP

## Перед запуском

### 1. Проверьте наличие API ключа

```bash
# Проверьте backend/.env
cat backend/.env | grep -E "(GEMINI|GROQ|OPENAI)_API_KEY"
```

Должен быть хотя бы один из:
- ✅ `GEMINI_API_KEY=...` (рекомендуется, бесплатный tier)
- ✅ `GROQ_API_KEY=...` (альтернатива)
- ✅ `OPENAI_API_KEY=...` (альтернатива, платный)

Если нет, добавьте:
```bash
echo "GEMINI_API_KEY=your_key_here" >> backend/.env
```

### 2. (Опционально) Проверьте Redis

```bash
# Проверьте что Redis запущен
redis-cli ping
# Должно вернуть: PONG
```

Если нет:
```bash
# Установите и запустите Redis
brew install redis  # macOS
brew services start redis

# или через Docker
docker run -d -p 6379:6379 redis
```

Redis не обязателен, но улучшает производительность через кеширование.

## Тестирование

### 1. Проверьте синтаксис кода

```bash
cd backend
source venv/bin/activate
python -m py_compile services/agent_service.py
python -c "from services.agent_service import SimpleAgent; print('✅ OK')"
```

### 2. (Опционально) Запустите тесты

Только если у вас настроен API ключ:

```bash
cd backend
source venv/bin/activate
python test_agent.py
```

Ожидаемый результат:
```
✅ Все тесты пройдены успешно!
Итого: 3/3 тестов пройдено
```

### 3. (Опционально) Запустите демо

```bash
cd backend
source venv/bin/activate
python demo_agent.py
```

Покажет сравнение обычной генерации vs агента.

## Frontend интеграция

### Вариант 1: Простой (для тестирования)

Добавьте checkbox в форму генерации:

```jsx
const [useAgent, setUseAgent] = useState(false);

<label>
  <input
    type="checkbox"
    checked={useAgent}
    onChange={(e) => setUseAgent(e.target.checked)}
  />
  🤖 Использовать AI-агента (улучшенное качество)
</label>
```

При отправке запроса:

```javascript
fetch('/generate-map', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${token}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    text: requirements,
    use_agent: useAgent  // ← Передаем параметр
  })
})
```

### Вариант 2: A/B тестирование

```javascript
// 50% пользователей с агентом, 50% без
const useAgent = Math.random() > 0.5;

fetch('/generate-map', {
  method: 'POST',
  body: JSON.stringify({
    text: requirements,
    use_agent: useAgent
  })
})
```

### Вариант 3: Всегда включен (после успешных тестов)

```javascript
// По умолчанию всегда используем агента
fetch('/generate-map', {
  method: 'POST',
  body: JSON.stringify({
    text: requirements,
    use_agent: true  // Всегда
  })
})
```

### Показ метрик пользователю (опционально)

```jsx
{response.agent_metadata && (
  <div className="agent-stats">
    <h4>📊 Метрики генерации</h4>
    <p>
      Качество: {(response.agent_metadata.validation.score * 100).toFixed(0)}%
    </p>
    <p>
      Время: {response.agent_metadata.metrics.total_time.toFixed(1)}s
    </p>
    {response.agent_metadata.metrics.fix_attempted && (
      <p className="success">
        ✅ Автоматически исправлено {response.agent_metadata.metrics.critical_issues_before_fix} ошибок
      </p>
    )}
  </div>
)}
```

## Сбор метрик

### Неделя 1: Сбор данных

Логируйте следующие метрики для каждого запроса:

```javascript
// После получения ответа
if (response.agent_metadata) {
  // С агентом
  analytics.track('map_generated', {
    use_agent: true,
    validation_score: response.agent_metadata.validation.score,
    generation_time: response.agent_metadata.metrics.total_time,
    fix_attempted: response.agent_metadata.metrics.fix_attempted,
    issues_fixed: response.agent_metadata.metrics.critical_issues_before_fix
  });
} else {
  // Без агента
  analytics.track('map_generated', {
    use_agent: false
  });
}
```

### Неделя 2: Анализ

Сравните метрики:

```python
# Пример анализа
without_agent = {
    "avg_score": 0.75,      # Примерная оценка
    "avg_time": 10.2,
    "success_rate": 0.82
}

with_agent = {
    "avg_score": 0.88,      # Из validation.score
    "avg_time": 12.5,       # Из metrics.total_time
    "success_rate": 0.91     # Процент is_valid: true
}

improvement = (with_agent["avg_score"] - without_agent["avg_score"]) / without_agent["avg_score"] * 100
print(f"Улучшение качества: +{improvement:.1f}%")

# Если улучшение > 10% → успех!
```

## Критерии успеха

Агент считается успешным если:

- ✅ Validation score улучшился на **10%+**
- ✅ Время генерации приемлемо (<20 секунд)
- ✅ Пользователи довольны качеством
- ✅ Процент успешных генераций >90%

## Если что-то пошло не так

### Ошибка: "No AI providers configured"

**Причина:** Нет API ключа в .env

**Решение:**
```bash
echo "GEMINI_API_KEY=your_key" >> backend/.env
# Перезапустите сервер
```

### Ошибка: "Redis unavailable"

**Причина:** Redis не запущен

**Решение:** Агент работает без Redis, но медленнее (нет кеша).
Чтобы включить кеш:
```bash
brew services start redis  # macOS
# или
docker run -d -p 6379:6379 redis
```

### Агент слишком медленный

**Решение 1:** Отключите исправление ошибок
```python
# В agent_service.py, функция generate_map_with_agent:
agent = SimpleAgent(
    enable_validation=True,
    enable_fix=False  # ← Отключить исправление
)
```

**Решение 2:** Используйте более быструю модель
```bash
# В backend/.env
GEMINI_GENERATION_MODEL=gemini-1.5-flash  # Быстрая модель
```

### Низкое качество генерации

**Решение:** Проверьте промпты в `agent_service.py`:
- Метод `_generate()` - промпт для генерации
- Метод `_fix()` - промпт для исправления

Можно улучшить инструкции или добавить примеры.

## Следующие шаги после успешного теста

Если агент показал улучшение качества на 10%+:

### Краткосрочные (1-2 недели):
1. ✅ Включить по умолчанию для всех пользователей
2. ✅ Добавить метрики в UI
3. ✅ Настроить мониторинг (Sentry, logs)

### Среднесрочные (1-2 месяца):
1. 🔄 Пошаговая генерация (Activities → Tasks → Stories отдельно)
2. 🔄 Function Calling для точной валидации
3. 🔄 WebSocket для прогресс-бара
4. 🔄 Множественные итерации исправления (до 3х)

### Долгосрочные (3+ месяца):
1. 🔮 Dashboard для мониторинга качества
2. 🔮 Semantic similarity проверка
3. 🔮 Интеграция с validation_service
4. 🔮 Проверка дубликатов

## Документация

- 📖 **Быстрый старт**: `backend/QUICK_START_AGENT.md`
- 📖 **Подробная документация**: `backend/AGENT_MVP_README.md`
- 📖 **Итоговый отчет**: `AGENT_SUMMARY.md`
- 📖 **Готовность к запуску**: `AGENT_READY.md`

## Контакты и поддержка

Вопросы? Создайте issue в репозитории или обратитесь к документации.

---

**Статус**: ✅ Готово к запуску
**Дата**: 2025-12-05
**Версия**: MVP 1.0

## Финальный чеклист перед запуском

- [ ] API ключ настроен в `backend/.env`
- [ ] (Опционально) Redis запущен
- [ ] Код проверен на синтаксис
- [ ] Frontend интегрирован (добавлен параметр `use_agent`)
- [ ] Настроена аналитика для сбора метрик
- [ ] Пользователи уведомлены о новой функции (опционально)
- [ ] План A/B тестирования готов

**🚀 Можно запускать!**
