# Список проектов пользователя

## 📋 Описание

Реализована функциональность для просмотра всех созданных проектов с поиском и фильтрами.

### Проблема
Пользователи теряли сгенерированные карты после refresh страницы.

### Решение
Создана страница со списком всех проектов пользователя. Клик по проекту открывает его карту.

---

## ✅ Реализованные возможности

### 1. **Список проектов**
- Отображение всех проектов пользователя в виде карточек
- Показывается название проекта и дата создания
- Пагинация (поддержка до 100 проектов)

### 2. **Поиск**
- Поиск проектов по названию в режиме реального времени
- Подсветка результатов поиска

### 3. **Сортировка**
- Сначала новые (по умолчанию)
- Сначала старые
- По алфавиту (по названию)

### 4. **Навигация**
- Клик по карточке проекта загружает его карту
- Кнопка "Создать новый проект" для создания нового проекта
- Кнопка "К списку проектов" для возврата из карты проекта
- Кнопка "Выйти" для выхода из системы

### 5. **UX улучшения**
- Красивые анимации hover эффектов
- Отображение времени создания проекта в удобном формате ("2 ч назад", "3 дн назад")
- Empty state когда нет проектов
- Состояние "Ничего не найдено" при неудачном поиске
- Индикатор загрузки

---

## 📁 Измененные файлы

### 1. **frontend/src/ProjectList.jsx** (НОВЫЙ)
Основной компонент для отображения списка проектов.

**Возможности:**
- Загрузка проектов через API `GET /projects`
- Поиск по названию проекта
- Сортировка (новые/старые/по имени)
- Клик по проекту загружает его через API `GET /project/{id}`
- Адаптивный grid layout (1/2/3 колонки)

### 2. **frontend/src/App.jsx** (ИЗМЕНЕН)
Добавлена интеграция компонента ProjectList.

**Изменения:**
- Добавлено состояние `view` для управления отображением ('list' или 'create')
- Новые функции:
  - `handleSelectProject()` - открывает выбранный проект
  - `handleCreateNew()` - переключает на форму создания
  - `handleBackToList()` - возврат к списку проектов
- Изменена логика отображения компонентов:
  1. Auth (если не авторизован)
  2. StoryMap (если выбран проект)
  3. ProjectList (если view === 'list')
  4. Форма создания (если view === 'create')

---

## 🎨 UI/UX особенности

### Список проектов
- Адаптивная сетка: 1 колонка на мобильных, 2 на планшетах, 3 на десктопах
- Hover эффекты с плавной анимацией
- Цветовые акценты (синий gradient для selected состояния)

### Карточка проекта
- Название проекта (крупным шрифтом)
- Дата создания (относительное время)
- Hover эффект с поднятием карточки (shadow-lg)
- Стрелка справа для указания кликабельности

### Empty states
- **Нет проектов:** иконка документа, призыв к действию "Создать первый проект"
- **Нет результатов поиска:** иконка поиска, предложение очистить фильтр

---

## 🔌 Backend API

Backend уже реализован и готов к использованию:

### GET /projects
Возвращает список проектов текущего пользователя.

**Query параметры:**
- `skip` (default: 0) - смещение для пагинации
- `limit` (default: 100) - количество проектов

**Ответ:**
```json
{
  "items": [
    {
      "id": 1,
      "name": "Название проекта",
      "created_at": "2025-11-25T10:30:00"
    }
  ],
  "total": 5,
  "skip": 0,
  "limit": 100
}
```

### GET /project/{project_id}
Возвращает полную структуру проекта для отображения карты.

**Ответ:** ProjectResponse с activities, tasks, stories, releases

---

## 🧪 Тестирование

### Запуск приложения

1. **Backend:**
```bash
cd backend
source venv/bin/activate
python main.py
# или
./start-backend.sh
```

2. **Frontend:**
```bash
cd frontend
npm install
npm run dev
# или
./start-frontend.sh
```

3. Откройте браузер: `http://localhost:5173`

### Сценарии для тестирования

#### ✅ Сценарий 1: Первый запуск
1. Зарегистрируйтесь или войдите
2. Увидите пустой список проектов
3. Нажмите "Создать первый проект"
4. Введите требования и сгенерируйте карту

#### ✅ Сценарий 2: Возврат к списку
1. После создания проекта нажмите "← К списку проектов"
2. Увидите созданный проект в списке
3. Кликните по карточке проекта
4. Проект должен загрузиться и открыться карта

#### ✅ Сценарий 3: Поиск
1. Создайте несколько проектов с разными названиями
2. В поле поиска введите часть названия
3. Список должен отфильтроваться в реальном времени

#### ✅ Сценарий 4: Сортировка
1. Создайте несколько проектов
2. Переключайте режимы сортировки
3. Проверьте что порядок меняется корректно

#### ✅ Сценарий 5: Refresh страницы
1. Откройте проект
2. Нажмите F5 (refresh)
3. После перезагрузки вы должны оказаться на списке проектов
4. Все проекты должны быть доступны

---

## 🎯 Решенная проблема

### До:
- После refresh страницы пользователь терял доступ к сгенерированной карте
- Не было способа просмотреть ранее созданные проекты
- Каждый раз нужно было генерировать карту заново

### После:
- ✅ Все проекты сохраняются в базе данных
- ✅ Список проектов доступен сразу после входа
- ✅ Можно вернуться к любому ранее созданному проекту
- ✅ Поиск и сортировка для удобной навигации
- ✅ Проекты не теряются при refresh страницы

---

## 🚀 Дальнейшие улучшения (опционально)

- [ ] Удаление проектов
- [ ] Редактирование названия проекта
- [ ] Дата последнего изменения
- [ ] Превью карты в карточке проекта
- [ ] Теги/категории проектов
- [ ] Экспорт проекта
- [ ] Поделиться проектом с другими пользователями
- [ ] Архивация старых проектов
- [ ] Дублирование проекта

---

## 📝 Примечания

- Компонент полностью адаптивен (mobile-first)
- Использует Tailwind CSS для стилизации
- Интегрирован с существующей системой авторизации
- Обрабатывает ошибки и показывает понятные сообщения
- Поддерживает автоматическое обновление токенов

---

**Автор:** AI Assistant  
**Дата:** 25 ноября 2025  
**Статус:** ✅ Готово к использованию

